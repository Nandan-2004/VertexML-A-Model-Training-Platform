# VertexML

A modular machine learning platform scaffold with preprocessing, modeling, tuning, evaluation, analysis, and UI components.

## Structure

- `app.py`: Entry point for the VertexML application.
- `core/`: Core ML logic and pipelines.
- `ui/`: UI components and layouts.
- `utils/`: Utility helpers and logging.
- `assets/`: Static assets such as styles.
