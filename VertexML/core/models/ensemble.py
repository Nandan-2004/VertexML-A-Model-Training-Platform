"""Ensemble model utilities for VertexML."""


def train_ensemble(models, X, y):
    """Train an ensemble model from base models."""
    return None


def ensemble_predict(ensemble_model, X):
    """Predict with an ensemble model."""
    return []
